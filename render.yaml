services:
  - type: web
    name: encogix-backend
    env: node
    plan: starter
    region: oregon
    
    # Don't use rootDir - we need to handle monorepo structure manually
    # rootDir: backend
    
    buildCommand: "npm ci --include=optional && npm run build"
    
    # Start command  
    startCommand: "npm start"
    
    # Auto-deploy from git
    autoDeploy: true
    
    # Health check endpoint
    healthCheckPath: /api/health
    healthCheckInterval: 30
    
    # Environment variables
    envVars:
      - key: NODE_ENV
        value: production

      - key: NEXT_PUBLIC_SITE_URL
        value: https://www.encogix.com

      - key: NEXT_PUBLIC_API_URL
        value: https://www.encogix.com

      - key: NEXT_PUBLIC_CASHFREE_ENV
        value: production
      
      - key: MYSQL_URL
        sync: false
      
      - key: ADMIN_USERNAME
        value: admin
      
      - key: ADMIN_PASSWORD
        sync: false
      
      - key: JWT_SECRET
        sync: false
      
      - key: CASHFREE_APP_ID
        sync: false
      
      - key: CASHFREE_SECRET_KEY
        sync: false

      - key: CASHFREE_ENV
        value: production

      - key: CASHFREE_RETURN_URL
        value: https://www.encogix.com/payment/success
      
      - key: BACKEND_PORT
        value: "5000"

